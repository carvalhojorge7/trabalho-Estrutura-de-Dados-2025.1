# Criando e populando tabela de hoteis
INSERT INTO hoteis (nome=Hotel Beira Mar, estrelas=4, cidade=Fortaleza);
INSERT INTO hoteis (nome=Hotel Centro, estrelas=3, cidade=Fortaleza);
INSERT INTO hoteis (nome=Pousada Praia, estrelas=2, cidade=Aracati);

# Consultando todos os hoteis
SELECT * FROM hoteis;

# Consultando hoteis de Fortaleza
SELECT * FROM hoteis WHERE cidade=Fortaleza;

# Atualizando estrelas de um hotel
UPDATE hoteis SET estrelas=5 WHERE nome=Hotel Beira Mar;

# Consultando após atualização
SELECT * FROM hoteis;

# Removendo hotel
DELETE FROM hoteis WHERE estrelas=2;

# Consultando após remoção
SELECT * FROM hoteis;
